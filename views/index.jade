doctype html
html(lang='en',class='ng-app')
  head
      title Welcome to Stockickr
      link(rel='stylesheet', href='/stylesheets/style.css')
      meta(charset='utf8')
      meta(name='viewport',content='initial-scale=1,width=device-width')
      meta(http-equiv='X-UA-Compatible',content='IE=edge,chrome=1')
      meta(name='google',content='notranslate')
      //Custom styles for this template
      link(href='blog.css', rel='stylesheet')
  body#container-fluid
    .sitewrap
    .main-nav#tiles
      .floating-logo.atomic-bomb-on-the-icon
    header#header
        .header-container.getdown3
          table.featuredText
             .strapline
                .logo
                  img.img-rounded.img-scale(src='/images/stockickr-logo-candidate.png', alt='logo')
                .title-container
                  .stockickr-at-the-top<span class=hasher>#</span>stockickr
                .intro-text welcome to our crowdfunding page                
    .bleedout.wam what is stockickr?
    .bleedout-white The first “true” real-time social trading platform, fully integrated with social media.
    .bleedout3.wam
    
            

    script(src='//ajax.googleapis.com/ajax/libs/jquery/1.8.0/jquery.min.js', type='text/javascript')
    script(src='/javascripts/scripts.js')    
    script.
      $('.parallax-section-1').parallax({
        speed : 0.2});
    script.
      tiles = $("#tiles").fadeTo(0, 0);

      $(window).scroll(function(d,h) {
          tiles.each.fadeIn(0.01) (function(i) {
              a = $(this).offset().top + $(this).height();
              b = $(window).scrollTop() + $(window).height();
              if (a < b) $(this).fadeTo(500,1);
          });
      });